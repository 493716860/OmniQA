# requirements.txt 文件说明
# -------------------------
# 后端 Python 直接依赖清单。
# 可按职责理解为：
# - Django / DRF：Web 服务与 REST API
# - Celery / redis：异步任务
# - openpyxl / PyYAML：Excel 与配置解析
# - requests / jsonpath / pactverify：接口执行、变量提取、断言
# - playwright：UI 自动化

Django==4.2.30
djangorestframework==3.15.2
celery==5.3.6
redis==5.0.8
PyMySQL==1.1.2
openpyxl==3.1.5
PyYAML==6.0.3
requests==2.32.5
jsonpath==0.82.2
pactverify==2.0.1
playwright==1.48.0
